import React from 'react';
import './title.scss';

const Title = ({name}) => {
    return (
        <div className="titleContainer">           
            <span>THT</span>
            <span>GUY</span>
            <span>JK.</span>
        </div>
    );
}

export default Title;
